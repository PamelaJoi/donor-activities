import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'donor activities',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
